<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>Streamdeck</title>
</head>
<body>
    <header>
        <h1>Streamdeck</h1>
    </header>
    <section class="scenes">
        {% for scene in scenes %}
            <div class="scene">
                <button form="change-scene">{{scene.scenename}}</button>

                <form action="/change-scene" method="post" id="change-scene">
                    <input class="hidden" name="scenename" value="{{scene.scenename}}" readonly>
                </form>
            </div>
        {% endfor %}
    </section>
</body>
</html>